#!/bin/zsh

export GOOGLE_APPLICATION_CREDENTIALS="/Users/username/Downloads/service-account-file.json"

alias run="docker compose exec -it app";
alias up="docker compose up app -d";
alias build="docker compose build app";
alias fb="run firebase";
alias node="run node";
alias npm="run npm";